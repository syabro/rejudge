{
  "name": "rejudge-site",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "bun run build && bunx wrangler pages deploy dist --project-name=rejudge"
  },
  "devDependencies": {
    "astro": "7.1.3"
  }
}
